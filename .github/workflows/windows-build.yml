name: Windows Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write

jobs:
  build:
    name: Build Windows Excel add-in
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Build release package
        shell: pwsh
        run: ./scripts/build_windows.ps1 -Configuration Release

      - name: Upload Windows add-in artifact
        uses: actions/upload-artifact@v4
        with:
          name: ludicrous-windows-release
          path: |
            excel-addin/LudicrousSpeed.ExcelAddIn/bin/Release/net48/**
            target/release/ludicrous-cli.exe
            target/release/ludicrous_engine.dll
          if-no-files-found: error

      - name: Package Windows release zip
        # Also run on a manual dispatch. Gating solely on 'push' meant a
        # hand-triggered build compiled fine and then silently skipped
        # packaging and releasing, producing no zip -- which looks like a
        # successful run that just didn't do the thing you wanted.
        if: >-
          github.event_name == 'workflow_dispatch' ||
          (github.event_name == 'push' && github.ref == 'refs/heads/main')
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $shortSha = "${{ github.sha }}".Substring(0, 7)
          $dist = "dist"
          $staging = Join-Path $dist "ludicrous-windows-$shortSha"
          New-Item -ItemType Directory -Force -Path $staging | Out-Null
          Copy-Item "excel-addin/LudicrousSpeed.ExcelAddIn/bin/Release/net48/*" $staging -Recurse -Force
          Copy-Item "target/release/ludicrous-cli.exe" $staging -Force
          Copy-Item "target/release/ludicrous_engine.dll" $staging -Force
          Compress-Archive -Path (Join-Path $staging "*") -DestinationPath (Join-Path $dist "ludicrous-windows-$shortSha.zip") -Force

      - name: Create GitHub release for this commit
        # Also run on a manual dispatch. Gating solely on 'push' meant a
        # hand-triggered build compiled fine and then silently skipped
        # packaging and releasing, producing no zip -- which looks like a
        # successful run that just didn't do the thing you wanted.
        if: >-
          github.event_name == 'workflow_dispatch' ||
          (github.event_name == 'push' && github.ref == 'refs/heads/main')
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: auto-${{ github.sha }}
          RELEASE_TITLE: LudicrousSpeed Windows ${{ github.sha }}
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $shortSha = "${{ github.sha }}".Substring(0, 7)
          gh release create $env:RELEASE_TAG "dist/ludicrous-windows-$shortSha.zip" `
            --title "LudicrousSpeed Windows $shortSha" `
            --notes "Automatic temporary release for commit ${{ github.sha }}." `
            --target "${{ github.sha }}" `
            --latest
